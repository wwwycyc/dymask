Metadata-Version: 2.4
Name: dymask-refactor
Version: 0.1.0
Summary: Standalone refactor of DyMask support-line experiments
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: numpy==2.2.5
Requires-Dist: pandas==2.3.3
Requires-Dist: pyarrow==23.0.1
Requires-Dist: Pillow==12.0.0
Requires-Dist: scikit-image==0.25.2
Requires-Dist: torch==2.5.1
Requires-Dist: torchvision==0.20.1
Requires-Dist: diffusers==0.37.1
Requires-Dist: transformers==5.4.0
Requires-Dist: safetensors==0.7.0
Requires-Dist: accelerate
Requires-Dist: lpips
Requires-Dist: opencv-python
Requires-Dist: ipython
Requires-Dist: tensorboard
Requires-Dist: tqdm

# DyMaskRefactor

`DyMaskRefactor` 是可单独分发的 DyMask support-line 重构版。当前目标是把你现在主线实验所需的运行时、指标和第三方依赖都收口到这个目录里，让别人拿走这个目录后，装好环境、填好本地权重路径，就可以直接复现实验。

## 设计目标

- 不依赖旁边的旧 `DyMask/`
- 当前 support-line 主线和深化版本可以直接复跑
- Stable Diffusion、CLIP、DINO checkpoint 都通过配置指定
- `NTIP2P` 与 DINO 代码已 vendored 到 `DyMaskRefactor/third_party/`
- 保留统一 runner、统一输出格式、统一指标导出

## 当前覆盖的 support variants

- `baseline`
- `c1`, `c2`, `c3`, `c4`
- `c5`
- `c5h0`, `c5h1`, `c5h2`, `c5h3`, `c5h4`
- `c6`, `c6b`, `c6c`, `c6d`

## 安装

### 方式 1：conda

```bash
cd DyMaskRefactor
conda env create -f environment.yml
conda activate dymask-refactor
```

### 方式 2：editable install

```bash
cd DyMaskRefactor
python -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -e .
```

如果你要在 GPU 上跑，请把 `torch/torchvision` 调整成与你 CUDA 匹配的版本。当前 `requirements.txt` 和 `environment.yml` 记录的是这台机器上验证过的一组版本。

## 目录说明

```text
DyMaskRefactor/
  README.md
  pyproject.toml
  requirements.txt
  environment.yml
  configs/
    support_line_local.example.json
  third_party/
    NTIP2P/
    dino/
  config.py
  runtime.py
  metric_runtime.py
  support_line/
  run_support_line.py
```

## 需要你自己配置的路径

复制示例配置：

```bash
cp configs/support_line_local.example.json my_run.json
```

然后修改这些字段：

- `model_id`: Stable Diffusion 权重目录。可以是本地 diffusers 目录，也可以是 HF repo id。
- `clip_model_id`: CLIP 权重目录。支持本地目录，也支持 HF repo id。
- `piebench_path`: PIE-Bench 数据集目录。
- `output_root`: 输出根目录。
- `ntip2p_root`: 可选。默认优先使用 `DyMaskRefactor/third_party/NTIP2P`。
- `dino_model_name`: 当前默认 `dino_vitb8`。
- `dino_weights_path`: `structure distance` 使用的 DINO checkpoint 本地路径。

注意：`structure distance` 现在不会再偷偷走 `torch.hub` 下载。如果启用了该指标，就必须显式提供 `dino_weights_path`；否则会直接报错，避免不一致和不可复现。

## 运行方式

### 直接用 JSON 配置

```bash
python run_support_line.py --config-json my_run.json
```

### 命令行覆盖部分参数

```bash
python run_support_line.py   --config-json my_run.json   --row-indices 0 1 2 3 4 5 6 7   --num-inversion-steps 50   --num-edit-steps 50   --sample-batch-size 4
```

### 如果你是 editable install

```bash
dymask-support --config-json my_run.json
```

## 最小 smoke test

只验证配置解析、样本准备和 run 目录生成：

```bash
python run_support_line.py   --variant c5h4   --piebench-path /abs/path/to/PIE-Bench   --model-id /abs/path/to/sd15-diffusers   --clip-model-id /abs/path/to/clip-vit-large-patch14   --row-indices 0   --run-limit 1   --methods full_dynamic_mask   --dry-run
```

## 可复现输出

每次运行都会在 run 目录里保存：

- `config.json`
- `variant.json`
- `sample_manifest.json/csv`
- `metrics_case_level.csv`
- `metrics_summary.csv`
- 诊断图和 debug json

要复现实验，至少需要固定：

- 输入数据集
- `model_id`
- `clip_model_id`
- `ntip2p_root`
- `dino_weights_path`
- 运行配置 JSON
- 当前代码版本

## 已内置的第三方代码

- `third_party/NTIP2P`: Null-Text Inversion / Prompt-to-Prompt 相关最小运行时
- `third_party/dino`: structure distance 用到的 DINO ViT 最小实现

也就是说，`DyMaskRefactor` 当前已经不再依赖外部 `DyMask/`，也不再要求额外再摆一个 DINO repo。最终用户需要自己准备的，主要就是模型权重、数据集和运行配置。
